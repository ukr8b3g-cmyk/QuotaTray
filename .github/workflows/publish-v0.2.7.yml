name: publish-v0.2.7

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/publish-v0.2.7.yml'

permissions:
  contents: write

jobs:
  publish:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: main
          fetch-depth: 0

      - name: Remove one-shot workflow and finalize release target
        shell: pwsh
        run: |
          Remove-Item '.github/workflows/publish-v0.2.7.yml'

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add '.github/workflows/publish-v0.2.7.yml'
          git commit -m 'Finalize QuantaTray v0.2.7 release'
          git push origin HEAD:main

          $releaseTarget = git rev-parse HEAD
          Add-Content -Path $env:GITHUB_ENV -Value "RELEASE_TARGET=$releaseTarget"

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '10.0.x'
          cache: true
          cache-dependency-path: '**/*.csproj'

      - name: Restore and test
        shell: pwsh
        run: |
          dotnet restore QuantaTrain.slnx
          dotnet test QuantaTrain.slnx -c Release --no-restore

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup --yes --no-progress

      - name: Build v0.2.7 release assets
        shell: pwsh
        run: ./packaging/scripts/build-release.ps1 -Version '0.2.7'

      - name: Verify release assets
        shell: pwsh
        run: |
          $assets = @(
            'dist/QuantaTray-v0.2.7-win-x64-setup.exe',
            'dist/QuantaTray-v0.2.7-win-x64-portable.zip',
            'dist/SHA256SUMS.txt'
          )
          foreach ($asset in $assets) {
            if (-not (Test-Path $asset)) {
              throw "Missing release asset: $asset"
            }
          }

      - uses: actions/upload-artifact@v6
        with:
          name: release-v0.2.7-win-x64
          path: |
            dist/QuantaTray-v0.2.7-win-x64-setup.exe
            dist/QuantaTray-v0.2.7-win-x64-portable.zip
            dist/SHA256SUMS.txt

      - name: Publish GitHub Release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $assets = @(
            'dist/QuantaTray-v0.2.7-win-x64-setup.exe',
            'dist/QuantaTray-v0.2.7-win-x64-portable.zip',
            'dist/SHA256SUMS.txt'
          )

          gh release view 'v0.2.7' *> $null
          if ($LASTEXITCODE -eq 0) {
            gh release upload 'v0.2.7' $assets --clobber
            gh release edit 'v0.2.7' `
              --title 'QuantaTray v0.2.7' `
              --notes-file RELEASE_NOTES.md `
              --draft=false
          }
          else {
            gh release create 'v0.2.7' `
              --target "$env:RELEASE_TARGET" `
              --title 'QuantaTray v0.2.7' `
              --notes-file RELEASE_NOTES.md `
              $assets
          }
